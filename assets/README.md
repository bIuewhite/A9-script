# assets 目录

放**收款码图片**用的（控制台左下角「❤ 打赏作者 ❤」弹窗里显示的那张）。

- 文件名：`donate.png`（就是要放成 `assets/donate.png`）
- 换图：**直接替换这个文件**就行，界面里没有"更换图片"入口
- 想换成别的路径/文件名：改 `config.py` 里的 `DONATE_QR`

没有这张图也完全不影响使用，弹窗只会显示一句"还没放收款码图片"的提示。
