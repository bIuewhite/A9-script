# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)：
`major.minor.patch` —— 大改 / 加功能 / 修 bug。
版本号只写在 `version.py` 里，这里每次发版同步加一段。

---

## v1.0.1（2026-09-14）渠道服支持 + 体检修复

### 新功能

- **游戏包名自动识别**：国服《狂野飙车9》是分渠道发行的（TapTap / 4399 / 华为 /
  小米 / 应用宝 …），每个渠道包名都不一样。脚本启动时会自己问模拟器「装的是哪个
  版本」并**自动切换过去，不用改任何配置**；环境体检也会把真实包名和该填的值
  直接打出来。参数设置里新增「游戏包名 / 启动 Activity」两项（留空 = 全自动）
- **启动游戏更稳**：改成两段式 —— 先试 `am start -n <Activity>`，5 秒内没起来就用
  `monkey -p <包名>` 按包名拉起（不需要知道入口类），渠道服也能正常启动

### 修掉的问题

- **多模拟器时体检误报**：环境体检调 adb 时没有指定设备（少了 `-s`），只要同时开着
  两个模拟器（比如雷电 + MuMu），adb 就会报 "more than one device/emulator"，
  于是「分辨率」和「游戏安装」两项永远显示失败——看起来像"游戏没装"，实际是没指定
  设备。挂机本体没有这个问题（一直是带 `-s` 的），只影响体检显示。现在体检也会在
  检测到多个设备时明确提示，并告诉你脚本会用哪一个
- **`上传诊断.bat` 跑不起来**：脚本改名后 bat 里还写着旧的中文文件名
- 单元测试 64 → **87 个**（新增渠道服识别、优先级、Activity 解析、设备异常等用例）

---

## v1.0.0（2026-09-12）首个公开版本

### 功能

- **一键配置环境**：新用户双击 `一键配置.bat` 即可 —— 自动找 Python（找不到会提示并
  打开下载页）、装依赖（清华镜像优先，自动回退官方源）、检查 tkinter、
  最后跑一遍环境体检
- **`启动控制台.bat`**：桌面控制台的图形界面入口。仓库里不带 exe，
  双击这个 bat 就能打开控制台（自动找 `pyw`/`pythonw`，不留黑窗口；
  找不到 Python 会提示先跑一键配置）
- **环境体检**（`doctor.py`，`python main.py check`）：逐项检查 Python 版本、依赖、
  adb、模拟器连接、分辨率是否 1920×1080 横屏、游戏是否安装/在前台、模板是否齐全，
  每项都给出可执行的下一步提示
- **全流程自动挂机**：模式选择 → 主界面 → 选车 → 匹配 → 比赛 → 赛后结算 → 弹窗，循环执行
- **桌面控制台**（Tkinter，非网页）：状态总览 / 选车管理 / 参数设置 / 实时日志 / 模板状态
- **截图选车**：在控制台的「选车管理」页点一下就能抓模拟器画面，**在界面里直接框选**
  添加车辆（不用先在外面截图再选文件），存好后自动做质量体检
  （框太小 / 框到空白 / 框到到处都是的通用 UI 元素都会提醒）
- **打赏入口**：控制台左下角「❤ 打赏作者 ❤」，点开是作者的话 + 收款码
- **动态奖励处理**：点完『错失机会』后弹出的「动态奖励」界面（从一组卡片里选一个），
  支持两种模式 —— **自动选第 N 张**（参数设置里可调，1~5）或**弹窗提醒你自己选**
  （提醒窗置顶 + 响铃，选完自动关）。识别只认左上角标题，所以奖励内容每次都换也能认
- **界面识别 + 断点续跑**：启动时先认出现在停在游戏哪一屏，从那一屏接着跑；
  手动点到流程中途再启动也能接上
- **按图片选车**：车辆用整车卡模板识别，不依赖坐标；没油自动换下一辆
- **闪退自动恢复**：检测到游戏不在前台就重启游戏并重新走流程
- **氮气自动化**：按坐标每 18 秒双击一次，双击间隔 570–590ms 随机
- **赛后弹窗处理**：里程碑奖励 / 升段 / 降段（可能同时出现，依次处理）
- **图文使用教程**：README 最前面就是「图文使用教程」（第一次使用 / 选择自己的车 /
  已知限制，含 4 张插图），原来的 Word 版 `A9控制台使用教程.docx` 也一起放在仓库里，
  方便打印或发群
- 模板 / 坐标 / 车卡采集工具，64 个离线单元测试 + GitHub Actions

### 这一版修掉的关键问题

- **参数页右边看不见**：画布里的内嵌 frame 没有钉住宽度，它按内容自然宽度显示
  （实测 1415px），而画布只有 1276px —— 右侧 139px 被裁掉，又没有横向滚动条，
  用户永远够不着。现在把内嵌宽度钉成画布宽度，并让长提示文字自动换行
  （否则 3 列会被撑得比画布还宽）；顺手修了鼠标滚轮一上来就 `bind_all`
  导致「滚动会串到别的页面」的问题。`tests/test_gui_layout.py` 盯着这两条。
- **匹配失败后重试无效，白等一整场**：匹配失败（服务器错误弹窗）时游戏会把你
  **退回选车界面**，那里没有详情页的『开始』按钮；旧代码关掉弹窗后直接去找
  『开始』，必然失败（日志：「没找到『开始』按钮，无法重新发起匹配」），
  然后就白等一轮进"比赛"，实际什么都没跑。现在会先识别当前界面：
  还在详情页就点『开始』，已退回选车界面就**重新选一次车**再匹配；
  连续失败 5 次才放弃本场，且没匹配上就不再空跑赛后流程。

- **选车点错车（点到了相邻的车）**：进入选车界面后车辆列表会横向滑动几秒
  （实测约 570–800 px/s），旧版在滑动途中取坐标，等点击发出去时车已经滑走一格。
  现在取坐标前先确认画面完全静止（帧差判据：静止 0.3–0.4 / 滚动中 46.7），
  并连拍两帧确认坐标一致。
- **把别的车当成目标车**：车卡模板里有大片「所有车都一样」的装饰，
  实测别的车能匹到 0.83（目标车是 0.99）。车辆匹配改用独立的更严阈值 0.92，
  并加了「最佳位置必须比次佳高出 0.05」的歧义保护。
- **车卡被屏幕边缘裁掉就找不到**：新增自动翻页找车（前后各最多 3 页，找完翻回原位）。
- **加载中截图把游戏压崩**：实测游戏加载时截图每次约 43 秒必崩；
  现在重启后静默 75 秒、比赛开局 20 秒完全不截图。
- **赛后弹窗漏处理**：主界面会先渲染、弹窗后加载，改成连续确认多次无弹窗才结束。
- **闪退恢复判断失效**：原先依赖的「登陆成功标志」模板实际采错成了模式选择界面的
  「多人游戏」标签（会匹到 0.998 造成误判），已删除；改为用界面识别判断加载完成。
- **选车界面不在状态机里**：模式选择界面（游戏最外层）以前没人管，停在那里会干等，
  现在会自己点进经典系列赛。
- **`.bat` 在中文系统上跑不起来**：之前所有 bat 都是纯 LF 换行、且含 UTF-8 中文，
  而 cmd.exe 用启动时的代码页（中文系统 936）解析整个文件，结果中文被解码乱掉、
  把后面的命令字符也一起吃掉（`python gui.py` 变成 `.py' is not recognized`）。
  现在全部改成 CRLF + 纯 ASCII，中文提示交给 Python 打印，并加了测试防止再犯。
  另外加了 `.gitattributes` 把 `*.bat` 钉死为 `eol=crlf` —— 否则 Windows 上的
  `core.autocrlf=true` 会在提交时把 CRLF 转回 LF，别人 clone 或 Linux CI 上拿到的
  又是坏文件。

### 已知限制

- **比赛进行中无法接管**：这一屏没有可识别的标志，脚本会安静等待而不是乱点坐标
  （想在比赛中间也能接上，需要额外提供一张比赛画面的标志图）
- 模板与游戏 UI 版本绑定，游戏更新后可能需要重新采集
